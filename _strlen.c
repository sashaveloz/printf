#include "holberton.h"

/**
 *_strlen - Retunrd the lenght of a string.
 *@s: char *
 *Return: length (count)
 */

int _strlen(char *s)
{
	int count;

	while (s[count] != '\0')
	{
		a++;
	}
	return (count);
}
